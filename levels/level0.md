# Bandit Level 0 → Level 1

## Objective

Find the password stored in the file named `readme`.

## Commands Used

```bash
pwd
ls
cat readme
```

## Solution Process

Listed the files in the home directory using `ls`, found the file `readme`, and displayed its contents using `cat readme`.

## Concepts Learned

- Using `pwd` to check the current directory
- Using `ls` to list files
- Using `cat` to read file contents

-

## Password

[REDACTED]